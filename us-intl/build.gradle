apply plugin: 'com.android.application'

android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 29
        versionCode 1
        versionName "1.0.0"
    }

    signingConfigs {
        // Created via properties file by the root build.gradle
        release { }
    }

    buildTypes {
        release {
            // signingConfig added via properties file by the root build.gradle
        }
    }
}
